<h1> All Genres </h1>

<ul>
    <% @genres.each do |genre| %>
        <li><%= link_to genre.name, genre_path(genre) %></li>
    <% end %>
</ul>
<%=button_to "Create New Genre", new_genre_path, method: :get %> <br>
<%=button_to "Return to Homepage", home_path, method: :get %> <br>